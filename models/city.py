#!/usr/bin/python3
"""This is the main file for the city model."""
from models.base_model import BaseModel


class City(BaseModel):
    """This is the main class for the city model."""

    state_id = ""
    name = ""